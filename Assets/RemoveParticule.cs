using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RemoveParticule : MonoBehaviour
{
    [SerializeField]
    ParticleSystemForceField _ForceField;
    // Update is called once per frame
    void Update()
    {
    }
}
